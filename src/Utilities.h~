


typedef uint32_t index; // more expressive name for an index into an array
typedef uint32_t count; // more expressive name for an integer quantity
typedef index node; 
typedef double edgeweight; // edge weight type
